# Read a student text submission

Reads a plain-text student answer from a `.txt`, `.md`, or `.pdf` file
and returns it as a single character string.

## Usage

``` r
read_student_text(path)
```

## Arguments

- path:

  Path to the submission file.

## Value

A single character string containing the file contents.
